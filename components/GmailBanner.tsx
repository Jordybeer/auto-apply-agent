'use client';

/**
 * GmailBanner is disabled — email sending now goes via Resend (info@jordy.beer).
 * File kept so existing imports don't break. Renders nothing.
 */
export default function GmailBanner() {
  return null;
}
